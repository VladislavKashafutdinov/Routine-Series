# TODO

## Взять в работу

### 1. SeriesDefinition — модель и миграция
- [x] Добавить тип `SeriesDefinition` в types/index.ts
- [x] Убрать `seriesLength`, `reward`, `currency` из `Activity`
- [x] Добавить таблицу `seriesDefinitions` в db.ts (миграция v1→v2)
- [x] Миграция: перенос существующих параметров из Activity в SeriesDefinition
- [x] `addActivity`: создаёт Activity + первый SeriesDefinition

### 2. Архивация активностей
- [x] Добавить `archived: boolean` в `Activity`
- [x] Логика удаления: hard-delete при отсутствии Completion и RewardIssue, иначе `archived = true`
- [x] Фильтрация архивных активностей в дашборде и мониторинге

### 3. Главная страница — Активности сегодня
- [x] `PageTabs` — переключение страниц в шапке
- [x] `AddActivity` — форма: название + длина/награда/валюта
- [x] `Dashboard` — секции «Не выполнено» / «Выполнено»
- [x] `ActivityCard`:
  - [x] Редактируемое название
  - [x] `SeriesProgress` — квадратики прогресса (без кликов)
  - [x] Кнопка «Отметить» / «Отменить»
  - [x] Кнопка удаления (с подтверждением)

### 4. Перемотка времени
- [x] `TimeOffsetContext` — смещение в днях, провайдер
- [x] `TimeTravel` — кнопки ◀ дата ▶ «Сегодня» в шапке
- [x] `today(offset)` и `getDateRange(days, offset)` — поддержка смещения
- [x] Проброс offset во все компоненты

### 5. RewardIssue — модель
- [x] Добавить тип `RewardIssue` в types/index.ts
- [x] Добавить таблицу `rewardIssues` в db.ts
- [x] Методы в хуке: создать, удалить, редактировать RewardIssue

### 6. Вычисление серий
- [x] Реализовать алгоритм из README («Как формируются серии»)
- [x] Статусы: активна / завершена / прервана
- [x] Пересчёт при изменении Completion, SeriesDefinition, смене даты

### 7. Расчёт наград
- [x] Награда за завершённую серию = reward из связанного SeriesDefinition
- [x] Общая начисленная = сумма RewardIssue
- [x] Неначисленная = сумма завершённых − начисленная

### 8. SeriesWidget — интерактивный виджет серии
- [x] `SeriesWidget` — квадратики с кликами + даты старта/конца
- [x] Клик на первый серый → отметить день
- [x] Клик на последний зелёный → отменить день
- [x] Будущие даты не кликабельны

### 9. Апгрейд Node.js до v26
- [x] Установить Node.js 24 LTS (до 26 не дотянули, но structuredClone есть)
- [x] Проверить `npm run build` (tsc + vite)
- [x] Проверить `npm run lint` (ESLint)
- [x] Проверить `npm run test` (Vitest)
- [x] Проверить `npm run dev` (Vite HMR)

### 10. Страница мониторинга

#### 10a. MonitoringPage + ActivityAccordion
- [x] `MonitoringPage` — контейнер: заголовок «Мониторинг», список `ActivityAccordion` (по одному на активность)
- [x] `ActivityAccordion` — раскрывающийся блок:
  - [x] Заголовок: название активности, `RewardCounters` (начислено/неначислено/всего заработано), кнопка «Начислить» (пока заглушка)
  - [x] Переключение вкладок: «История серий» / «История начислений»
  - [x] Состояние: открыт/закрыт, по умолчанию все закрыты, только один открыт одновременно

#### 10b. TimeTravel — виртуальное «сегодня»

TimeTravel: должен управлять не смещением относительно сегодня, а самим виртуальным «сегодня»
  - [x] добавить VirtualTodayContext (virtualToday, setVirtualToday)
  - [x] при изменении offset в TimeTravel должен изменяться virtualToday в VirtualTodayContext. TimeTravel одновременно работает с TimeOffsetContext и VirtualTodayContext
  - [x] заменить все использования offset из TimeOffsetContext во всех компонентах на virtualToday из VirtualTodayContext
  - [x] если после предыдущих шагов TimeOffsetContext используется только в TimeTravel, то удалить TimeOffsetContext с соответствующей модификацией TimeTravel
  - [x] проверить использование функции today во всем коде. заменить на использование useVirtualToday
  - [x] заменить все использования реальной даты/времени во всех компонентах на virtualToday из VirtualTodayContext. Исключение - Activity.createdAt.

#### 10c. Вкладка «История серий»
- [x] `computeSeries` для активности → массив `ComputedSeries`
- [x] Список `SeriesWidget` (уже создан в задаче 8)
- [x] При наведении на квадратик в SeriesWidget — показывать дату (уже есть `title={d}`)
- [x] Увеличить отступ между SeriesWidget когда несколько серий (квадратики налезают друг на друга)
- [x] Серии должны визуально отличаться друг от друга по своему `status`
- [x] Пагинация: по N серий на страницу, кнопки «Назад» / «Вперёд»
- [x] Поехала верстка в SeriesWidget: swidget__squares должен быть по центру относительно
- [x] Если серий нет — заглушка «Серий пока нет»

#### 10d. Вкладка «История начислений»

##### 10d-i. `RewardHistoryTab` — контейнер
- [ ] Получить `RewardIssue[]` для активности (из `useActivities` или отдельным запросом)
- [ ] Если начислений нет — заглушка «Начислений пока нет»
- [ ] Пагинация через `Paginator` (переиспользовать)

##### 10d-ii. Таблица `RewardIssue`
- [ ] Колонки: дата | сумма | валюта | действия
- [ ] Строки сортированы по дате (новые сверху)

##### 10d-iii. Inline-редактирование
- [ ] Клик по ячейке даты/суммы/валюты → появляется input
- [ ] Enter или blur → сохранить через `updateRewardIssue`
- [ ] Escape → отменить, вернуть исходное значение

##### 10d-iv. Удаление
- [ ] Кнопка удаления в каждой строке
- [ ] Подтверждение перед удалением
- [ ] Удаление через `deleteRewardIssue`

#### 10e. Кнопка «Начислить»
- [ ] Форма/модалка: дата (по умолчанию сегодня), сумма (предзаполнена из `totalUnissued` или `reward`), валюта
- [ ] Сохранение → `addRewardIssue`
- [ ] После сохранения — форма закрывается, таблица обновляется

### 11. Архив
- [ ] `ArchivePage` — список архивных активностей
- [ ] Кнопка «Восстановить» у каждой

### 12. Экспорт / Импорт
- [ ] Экспорт: все таблицы БД → JSON-файл, скачивание
- [ ] Импорт: выбор файла → валидация → замена данных (с подтверждением)

### 13. Редактирование SeriesDefinition
- [ ] `EditSeriesDefinition` — форма в ActivityAccordion на странице мониторинга
- [ ] Поля: длина серии, награда, валюта (предзаполнены текущими)
- [ ] Сохранение → новый SeriesDefinition, старый не меняется

---

## Отложено

- [ ] Обновиться до Node.js 26 LTS (сейчас v24)
- [ ] Заменить × на иконку корзины в кнопке удаления ActivityCard
- [ ] TimeTravel: сделать скрываемым, менее заметным, компактным — это инструмент тестирования, а не пользовательский
- [ ] SeriesWidget: странное поведение при добавлении/удалении completions (неочевидное обновление)

### Баги

<!-- Описание бага → поиск причины → пошаговая инструкция исправления → добавляется в «Взять в работу» -->
- Разрывы в SeriesProgress на основной странице при перемотке времени: если при перемотке не отметить какую-либо активность как выполненную, то далее если перематывать время вперед в SeriesProgress появляются перерывы в последовательности зеленых квадратиков. Вероятно связано с неправильным TimeTravel-ом, либо с неправильным расчетом активной серии.
- Пропадает форма для SeriesDefinition после добавления активности