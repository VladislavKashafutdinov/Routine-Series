# TODO

## Взять в работу

### 13. SeriesDefinitionTab — вкладка в ActivityAccordion
- [x] Заменить `EditSeriesDefinition` (кнопка в теле аккордеона) на вкладку
- [x] Список всех SeriesDefinition для активности (длина / награда / валюта / дата создания)
- [x] Кнопка/форма добавления нового SeriesDefinition внутри вкладки

### 14. CompletionsTab — вкладка в ActivityAccordion
- [x] Календарь completions по месяцам (по 3 месяца на экране)
- [x] Пагинация между блоками по 3 месяца
- [x] Клик по дню — тоггл completion (добавить/удалить)

### 15. Группировка серий по SeriesDefinition
- [x] В SeriesHistoryTab: серии группируются под заголовком SeriesDefinition
- [x] Заголовок группы: длина / награда / валюта / дата создания SeriesDefinition

### 16. Мультивалютные счётчики наград
- [x] Добавить `earnedByCurrency`, `issuedByCurrency`, `unissuedByCurrency` в модель
- [x] `RewardCounters`: показывать earned/issued/unissued по каждой валюте отдельно
- [x] `IssueRewardModal` — сумма по умолчанию: unissuedByCurrency для валюты текущего SeriesDefinition
- [x] `RewardCounters` — убрать fallback на total-поля, показывать всегда через ByCurrency
- [x] Заменить все использования `totalEarned/Issued/Unissued` на ByCurrency-поля
- [x] Удалить `totalEarned`, `totalIssued`, `totalUnissued` из `ActivityWithStreak`
- [x] Расчёт наград по каждой валюте согласно VISION.md § «Как считается награда»
- [x] Счетчики по каждой валюте в `RewardCounters` должны находиться на отдельных строках

### 17. Привести хедер аккордеона в соответствие с VISION
- [x] Показывать только unissued > 0 per-currency (не earned/issued всегда)
- [x] Кнопка «Начислить» для каждой валюты с unissued > 0 (вместо одной общей)
- [x] Отображение активной серии в хедере (см. виджет серии)

### Доработка виджета серии

#### 1. Виджет только для отображения
- [x] Убрать кликабельность квадратиков (отметка/отмена теперь в CompletionsTab)
- [x] Убрать `useActivities` и `toggleDate` из `SeriesWidget`
- [x] Убрать импорт и использование `useVirtualToday`

#### 2. Изменить входные параметры
- [x] Принимать `startDate`, `seriesLength`, `completions` вместо `ComputedSeries`
- [x] Рассчитывать статус самостоятельно (active / completed / broken) на основе переданных данных
- [x] Рассчитывать даты окна серии от `startDate` до `startDate + seriesLength - 1`

### Доработка аккордиона Страница «Мониторинг»
- [x] Если нет активных серий, то в хэдере все равно должен отображаться SeriesWidget с пустой серией, с началом в текущей дате и длиной из текущего `SeriesDefinition`

- [x] Баг: при перемотке серия в хэдере аккордеона не обновляется. Решение: `computeSeries` перенесён в `ActivityAccordion`, используется «текущая» серия вместо «активной»

### 18. RewardCounters в RewardHistoryTab
- [x] Добавить `RewardCounters` в тело вкладки «История начислений» над таблицей
- [x] Счётчики показывают earned / issued / unissued по каждой валюте
- [ ] Для каждой валюты с unissued > 0 — кнопка/предложение «Начислить» (открывает `IssueRewardModal`)

### 19. Список предложений к начислению на Dashboard
- [ ] Баннер/секция над списком активностей
- [ ] Для каждой активности: unissued > 0 по валютам → кнопка «Начислить»
- [ ] Открывает `IssueRewardModal` с предзаполненной валютой

### 20. Экспорт / Импорт
- [ ] Экспорт: все таблицы БД → JSON-файл, скачивание
- [ ] Импорт: выбор файла → валидация → замена данных (с подтверждением)

---

## Отложено

- [ ] Обновиться до Node.js 26 LTS (сейчас v24)
- [ ] Заменить × на иконку корзины в кнопке удаления ActivityCard
- [ ] TimeTravel: сделать скрываемым, менее заметным, компактным — это инструмент тестирования, а не пользовательский
- [ ] SeriesWidget: странное поведение при добавлении/удалении completions (неочевидное обновление)
- [ ] Разместить компоненты вместе со стилями в отдельные папки
- [ ] Рутинг приложения
- [ ] Сделать визуальное оформление более невербальным (слишком много словесного описания, вместо визуального)
- [ ] Во вкладке календарь добавить обозначения дат (иконки/эмоджи), которые являются концом серии - разные обозначения для завершенных/прерванных серий

### Баги

<!-- Описание бага → поиск причины → пошаговая инструкция исправления → добавляется в «Взять в работу» -->
- Разрывы в SeriesProgress на основной странице при перемотке времени: если при перемотке не отметить какую-либо активность как выполненную, то далее если перематывать время вперед в SeriesProgress появляются перерывы в последовательности зеленых квадратиков. Вероятно связано с неправильным TimeTravel-ом, либо с неправильным расчетом активной серии.
- Пропадает форма для SeriesDefinition после добавления активности
