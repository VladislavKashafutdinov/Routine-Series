# TODO

## Общие задачи

- Импорт/экспорт всех данных в yaml
- Тест: нужна возможность "проматывать" время кнопкой - следующий день/предыдущий день

# Баги

- Все данные сбрасываются при перезапуске
- Если отметить последний день в серии в консоли появляется ошибка `"Readwrite transaction in liveQuery context. Querier source: async () => {\n      const [acts, sers, comps] = await Promise.all([\n        db.activities.toArray(),\n        db.series.toArray(),\n        db.completions.toArray()\n      ]);\n      const todayStr = today();\n      return acts.map((a) => buildActivityWithSeries(a, sers, comps, todayStr));\n    }"` 

## Серии активностей: 

- удаление и восстановление серий и активностей (можно случайно удалить активность или серию). Сделать через soft-delete технику.
- редактирование количества награды/валюты: количество и валюту можно редактировать отдельно для каждой серии
- отметка о выдаче награды
- при отметке сегодняшней активности наверх должна выезжать следующая незавершенная
- на сетке серии количество квадратиков должно быть равно количеству дней в серии
- редактирование количества дней серии для существующей активности. старые серии должны сохранить прежнее количество дней
- редактирование суммы награды для существующей активности. старые серии должны сохранить прежнюю сумму награды.
- редактирование валюты награды для существующей активности. старые серии должны сохранить прежнюю валюту

# Сводка

Нужна отдельная страница, на которой 
- я могу увидеть все завершенные/прерванные/незавершенные серии
- я могу увидеть все выданные и невыданные награды с датами выдачи

## Взять в работу
Серии активностей
- на сетке серии количество квадратиков должно быть равно количеству дней в серии