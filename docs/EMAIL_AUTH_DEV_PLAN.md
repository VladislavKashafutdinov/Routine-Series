# План реализации email-based авторизации

0. [Подготовка dev-среды](#0-подготовка-dev-среды)
1. [Базовая инфраструктура](#1-базовая-инфраструктура)
2. [Разработка авторизации](#2-разработка-авторизации). На этом этапе разработанное ранее апи и функции бэка остаются неизменны, манипуляции с данными происходят точно так же как есть. Просто авторизация.
3. [Модификация апи на привязку к пользователю](#3-модификация-апи-на-привязку-к-пользователю)
4. [Вливание в мастер и тестирование на master](#4-вливание-в-мастер-и-тестирование-на-master)

## ✅ 0. Подготовка dev-среды

- сделать ветку dev (прод живёт на master и используется — трогаем его только при вливании)
- сделать отдельную dev-БД (вторая БД на бесплатном хосте, если тариф позволяет; иначе локальный Postgres) + конфиги подключения в локальной среде разработки
- настроить локальный бэк на dev-БД
- добавить `http://localhost:5173` в разрешённые origins (CORS-конфиг)
- настроить локальный фронт на локальный бэк (адрес API в локальной среде)
- бэк и фронт разрабатываются и тестируются локально — деплой dev-ветки не делаем

## ✅ 1. Базовая инфраструктура

- [x] миграция в БД (разработка; накат на dev-БД произойдёт автоматически при старте локального бэка): таблицы `users`, `login_codes`, `sessions` + колонка `user_id` на `activities` (NULL у существующих строк — привязываются вручную в разделе 3)
- [x] настройка SMTP Gmail (app-пароль; Brevo отпал — телефонная верификация не проходит для моего номера)

## 2. ✅ Разработка авторизации

### ✅ Подготовка — каркас пакета auth
- [x] Создать каркас пакета авторизации (бэк — пакет auth): Handlers + загрузка конфигурации SMTP и сроков жизни сессий из env (дефолты: сессия 10 дней, refresh 30 дней) — фундамент для всех следующих шагов
- [x] Разрешить заголовок Authorization в CORS (бэк — CORS-конфиг) — без него фронт не сможет отправлять токены

### ✅ Фича 1 — проверка сессии при открытии приложения
- [x] Реализовать middleware авторизации (бэк — пакет auth, а не api: middleware ходит в БД за сессией, а auth-хендлеры импортируют api → был бы циклический импорт api↔auth): принимает Bearer-токен, находит сессию в БД, кладёт пользователя в контекст; без валидного токена отвечает 401
- [x] Добавить эндпоинт `GET /api/v1/auth/me` под этим middleware (бэк — хэндлер в пакете auth) — возвращает текущего пользователя
- [x] Создать контекст авторизации (фронт — hooks): при открытии приложения запрашивает /auth/me и по ответу выбирает состояние authenticated или unauthenticated
- [x] Создать заглушку страницы логина (фронт — компонент LoginPage)
- [x] Добавить гейт в приложение (фронт — App.tsx): неавторизованный пользователь видит заглушку логина, авторизованный — обычные страницы; данные грузятся только для авторизованного
- [x] Обновить схему компонентов в README (страница логина, контекст авторизации)
- [x] Проверить локально: открытие приложения → 401 → видна заглушка логина

### ✅ Фича 2 — отправка кода из формы логина
- [x] Реализовать валидацию запроса на отправку кода (бэк — модель запроса): корректный email, нормализация в нижний регистр
- [x] Реализовать отправку письма с кодом через SMTP Gmail (бэк — пакет auth, mail)
- [x] Реализовать генерацию 6-значного кода и хранение только его hash со сроком действия 10 минут; при новом запросе старые коды для этого email недействительны (бэк — запросы в БД)
- [x] Добавить эндпоинт `POST /api/v1/auth/code` (бэк — хэндлер) — отправляет код на любой валидный email (пользователь создаётся позже, при верификации), отвечает всегда 200, не раскрывая, существует ли email
- [x] Чекпоинт: проверить через Swagger, что письмо приходит — если не доходит, дальше не идём (единственная внешняя зависимость)
- [x] Добавить функцию запроса кода (фронт — src/api/auth.ts)
- [x] Заменить заглушку логина формой ввода email (фронт — компонент LoginPage): после отправки показывается заглушка формы ввода кода
- [x] Проверить локально: ввод email → письмо пришло → видна заглушка формы ввода кода

### ✅ Фича 3 — верификация кода и заведение сессии
- [x] Реализовать проверку кода (бэк — запросы в БД): совпадение с hash, срок действия, не более 5 попыток (после — код недействителен); при первом входе создать пользователя в БД
- [x] Реализовать создание сессии (бэк — запросы в БД): два случайных токена (access и refresh), в БД хранятся только их hash, сроки из конфигурации
- [x] Добавить эндпоинт `POST /api/v1/auth/verify` (бэк — хэндлер) — возвращает access-токен, refresh-токен и пользователя
- [x] Проверить через Swagger: верный код → токены выданы, в БД появились пользователь и сессия
- [x] Научить apiFetch отправлять заголовок Authorization, когда токен есть (фронт — src/api/fetch.ts)
- [x] Добавить функцию верификации кода (фронт — src/api/auth.ts); при успехе контекст авторизации сохраняет токены и переводит приложение в authenticated
- [x] Заменить заглушку формой ввода кода (фронт — компонент LoginPage): ошибки «неверный/устаревший код», повторная отправка, строки локализации en/ru
- [x] Проверить локально: вход по коду end-to-end; после перезагрузки страницы сессия восстанавливается (проверка из фичи 1 заработала по-настоящему)

### ✅ Фича 4 — продление и завершение сессии
- [x] Добавить эндпоинт `POST /api/v1/auth/refresh` (бэк — хэндлер) — обменивает refresh-токен на новую пару токенов (ротация)
- [x] Добавить эндпоинт `POST /api/v1/auth/logout` (бэк — хэндлер) — удаляет сессию
- [x] Научить apiFetch при 401 один раз обновлять токены через refresh и повторять запрос; если не вышло — переводить приложение в unauthenticated (фронт — src/api/fetch.ts)
- [x] Добавить кнопку выхода в интерфейсе (фронт — контекст авторизации + разметка): очистка токенов и запрос logout на сервер
- [x] Проверить локально: refresh продлевает сессию, выход удаляет её на сервере

### ✅ Фича 5 — защита от перебора
- [x] Добавить rate limit на отправку кода (бэк — пакет auth): по email и IP (например, 1 код в 60 сек на email, 10 в час на IP) — хранение в памяти достаточно для десятка пользователей
- [x] Обработать в форме логина ответ 429 при повторной отправке (фронт — компонент LoginPage): кнопка не даёт слать повторно и показывает ожидание
- [x] Проверить локально: повторная отправка сразу после первой → кнопка ждёт вместо спама письмами

## 3. ✅ Модификация апи на привязку к пользователю

Модифицируем апи манипуляций с данными на привязку к пользователю. Фронт не меняется — токен уже отправляется в каждом запросе.

- [x] Привязать всё апи данных к пользователю за один заход: RequireAuth на все маршруты /activities, /completions, /reward-issues, /series-definitions, /import (кроме /health — он нужен хосту); скопинг по user_id во всех db-запросах (activities напрямую, completions/reward_issues/series_definitions через JOIN с activities); create/update-хендлеры пишут user_id из контекста; swagger-аннотации 401 (привязка старых данных на dev-БД для проверки выполнена вручную)

## 4. ✅ Вливание в мастер и тестирование на master

Единственная проверка задеплоенного приложения — после вливания в master.

- [x] Настроить env-переменные на хосте: SMTP Gmail, сроки сессий, CORS-конфиг (продовый origin) — **итог по почте**: SMTP Gmail из Render заблокирован (dial i/o timeout, порты 587/465); Brevo отпал (телефон), SendGrid/Twilio отпал (регион) → отправка переводится на **Gmail API** (env: `GMAIL_API_CLIENT_ID`/`GMAIL_API_CLIENT_SECRET`/`GMAIL_API_REFRESH_TOKEN`, scope `gmail.send`, app в production без верификации). SendGrid-реализация оставлена в коде как запасная
- [x] Влить dev в master и дождаться деплоя бэка и фронта
- [x] Убедиться, что миграция 000002 накатилась автоматически при старте бэка
- [x] Привязать старые данные к своему пользователю:

```sql
UPDATE activities SET user_id = (SELECT id FROM users WHERE email = '<ваш email>') WHERE user_id IS NULL;
```
- [x] Проверить полный флоу на задеплоенном приложении: вход по коду, восстановление сессии, выход, защита от перебора